<?php
     $break = "<br>";
     // Control Structure are used to control the flow of a program based on specific conditions or logic

     // If-Else Statements: Used to execute different blocks of code based on a condition. 
     if (true) {
          echo(true);
     };
     echo($break);
     $var = null;
     if ($var) {
          echo("true");
     } else {
          echo("false");
     }
     echo($break);
     $var1 = 5;
     if ($var1) {
          echo("true");
     } else {
          echo("false");
     }
?>